# Dependencies
node_modules/

# ncc build artifact (generated by `npm run build`)
licenses.txt

# Logs & OS cruft
*.log
npm-debug.log*
.DS_Store

# Local env / secrets
.env
.env.*

# Note: dist/ is intentionally NOT ignored — GitHub runs the action from the
# committed bundle in dist/. Always `npm run build` and commit dist/ before tagging.
